[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "horencli"
version = "0.1.0"
description = "Herramienta CLI para scaffold de proyectos"
dependencies = [
    "typer>=0.9.0",
    "rich>=13.0.0"
]

[project.scripts]
horencli = "horen.main:app"
